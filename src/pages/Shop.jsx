import ProductList from '../components/ProductList';
import Section from '../components/UI/Section';

const Shop = () => {
  return (
    <Section>
      <ProductList />;
    </Section>
  );
};

export default Shop;
